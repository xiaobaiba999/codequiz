{
  "buildCommand": "pnpm exec prisma generate && mkdir -p public && echo OK > public/index.html",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/((?!api/health).*)", "destination": "/api/index" }
  ]
}
