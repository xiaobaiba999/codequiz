{
  "framework": null,
  "buildCommand": "npm install && npx prisma generate",
  "installCommand": "npm install",
  "outputDirectory": ".",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
