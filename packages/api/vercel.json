{
  "buildCommand": "pnpm exec prisma generate && mkdir -p public/api && cp api/*.ts public/api/",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
